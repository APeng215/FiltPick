package com.apeng.filtpick.keybinding;

import com.apeng.filtpick.Common;
import net.minecraft.client.KeyMapping;
import net.minecraft.resources.Identifier;

public class KeyBindManager {
    public static final KeyMapping.Category CATEGORY = new KeyMapping.Category(Identifier.fromNamespaceAndPath(Common.MOD_ID, Common.MOD_ID));
}
